# 🌯 BURЖУЙ Analytics Live Dashboard

Интерактивная панель мониторинга продаж, чеков, кассовых смен и персонала сети быстрого питания **«BURЖУЙ»** с прямой интеграцией с сервером **iikoRMS / iikoOffice API**.

---

## 🚀 Возможности

1. **Выбор любой торговой точки сети (Point Selector):**
   - Переключение между точками: `B-1`, `B-2`, `B-3`, `B-5`, `В-10`, `В-11`, `В-12`, `В-14`, `Б19 (Батыр Молл)` и др.
   - Режим сводного отчёта: **«⭐ Вся сеть (Сводный)»**.
2. **Гибкий выбор периода дат:**
   - Быстрые пресеты: *Сегодня*, *5 дней*, *7 дней*, *14 дней*, *Этот месяц*.
   - Произвольный выбор интервала дат (`с...` по `по...`).
3. **Ключевые KPI показатели:**
   - Выручка (со скидкой) в тенге (₸).
   - Количество пробитых чеков и гостей.
   - Средний чек (AVG).
   - Количество проданных блюд и среднее число блюд на заказ.
   - Общая сумма скидок.
4. **Графики и визуализации:**
   - Динамика ежедневных продаж и чеков с подсветкой пиковых дней.
   - Почасовое распределение выручки (24/7) с выявлением ночных и дневных пиков.
5. **Аналитика персонала и смен:**
   - Таблица кассиров: имя, табельный номер, количество чеков, выручка, личный средний чек и доля в кассе точки.
   - Журнал кассовых смен: номер смены, даты и время открытия/закрытия, менеджер, ответственный сотрудник, выручка (нал/безнал).
6. **Структура оплат и Доставка:**
   - Kaspi QR Интеграция, Наличные, Эквайринг (Береке Банк).
   - Агрегаторы доставки: Яндекс Еда, Wolt, Glovo.
7. **Топ блюд:**
   - Рейтинг блюд-локомотивов по выручке и объему продаж (Пита, Донеры, Шаурма и т.д.).
8. **Рейтинг всей сети:**
   - Таблица лидерборда всех точек за выбранный период с возможностью кликнуть на любую точку и мгновенно открыть её аналитику.

---

## 🛠 Стек технологий

* **Backend:** Node.js и Express с PostgreSQL, защищёнными личными кабинетами, задачами и кэшированием токенов iiko.
* **Безопасность:** HttpOnly-сессии на SHA-256, scrypt-пароли, RBAC, same-origin для mutating API, Helmet/CSP и rate limit входа.
* **Frontend:** Mobile-first SPA на чистом CSS, Chart.js и Vanilla JS.
* **Хостинг:** Render.com (готов к развертыванию через `render.yaml`).

---

## ⚙️ Переменные окружения (.env)

| Переменная | Описание | Пример |
| :--- | :--- | :--- |
| `DATABASE_URL` | PostgreSQL connection string | `postgresql://user:password@localhost:5432/burzhui` |
| `IIKO_HOST` | URL сервера iiko | `https://your-iiko-host.example` |
| `IIKO_LOGIN` | Логин пользователя iiko с доступом к API | `your_iiko_login` |
| `IIKO_PASSWORD` | Пароль пользователя iiko | `replace_with_iiko_password` |
| `APP_ADMIN_USERNAME` | Логин первого developer | `admin` |
| `APP_ADMIN_PASSWORD` | Пароль первого developer, минимум 8 символов | `replace_with_long_random_password` |
| `APP_ADMIN_NAME` | Имя первого developer | `BURЖУЙ Administrator` |
| `PORT` | Порт локального веб-сервера | `3000` |

---

## 📦 Локальный запуск

```bash
# Клонировать репозиторий
git clone https://github.com/ardigital221-cloud/burzhui-analytics.git
cd burzhui-analytics

# Установить зависимости (если есть npm)
npm install

# Скопировать .env.example в .env и заполнить локальными значениями.
# Запустить сервер
npm start
```

Сервер откроется по адресу: `http://localhost:3000`.

---

## 🌐 Развертывание на Render.com

Проект содержит готовый файл `render.yaml` (Blueprint):
1. Откройте [Render Dashboard](https://dashboard.render.com).
2. Нажмите **New +** -> **Blueprint**.
3. Выберите репозиторий `ardigital221-cloud/burzhui-analytics`.
4. Нажмите **Apply** — сервис автоматически соберется и запустится на бесплатном тарифе Render.
